{
    "name": "ffmpeg-wasm-demo",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "prepare:ffmpeg": "node scripts/prepare-ffmpeg-assets.mjs",
        "dev": "npm run prepare:ffmpeg && vite",
        "build": "npm run prepare:ffmpeg && vite build",
        "preview": "npm run prepare:ffmpeg && vite preview"
    },
    "dependencies": {
        "@ffmpeg/core": "^0.12.10",
        "@ffmpeg/ffmpeg": "^0.12.15",
        "@ffmpeg/util": "^0.12.1"
    },
    "devDependencies": {
        "vite": "^7.1.0"
    }
}

## 依赖版本冲突（最常见的坑）

**问题表现：**
项目安装新依赖后，控制台报 Cannot read property 'xxx' of undefined、Version Mismatch 等错误，或功能莫名失效，核心原因是不同依赖包依赖了同一个基础包的不同版本，导致最终安装的版本不兼容。

**解决方式**
查看包的依赖关系

```sh
npm ls xx
```

尝试统一版本

或者通过 package.json resoltion 强制统一版本

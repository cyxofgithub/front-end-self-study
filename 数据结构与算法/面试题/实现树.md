## 实现树

```javascript
class TreeNode<T> {
    val: T;
    children: TreeNode<T>[];
    constructor(val: T) {
        this.val = val;
        this.children = [];
    }
    addChild(val: T) {
        const node = new TreeNode() < T > val;
        this.children.push(node);

        return node;
    }
}

class Tree<T> {
    root: TreeNode<T>;
    constructor(val: T) {
        this.root = new TreeNode() < T > val;
    }

    dfs() {
        let reulst = null;
        const traverse = (node: TreeNode<T>) => {
            if (!node) return;
            const { val, children } = node;
            console.log('🚀 ~ Tree<T> ~ dfs ~ val:', val);
            children.forEach(item => traverse(item));
        };

        traverse(this.root);

        return reulst;
    }

    bfs() {
        const queue = [this.root];

        while (queue.length) {
            const cur = queue.shift();
            const { val, children = [] } = cur || {};
            console.log('🚀 ~ Tree<T> ~ bfs ~ val:', val);

            children.forEach(item => queue.push(item));
        }
    }
}

const tree = new Tree() < number > 1;
const root = tree.root;
root.addChild(2);
root.addChild(3).addChild(8);
root.addChild(4);

console.log('🚀 ~ root:', root);

tree.dfs();
tree.bfs();
```

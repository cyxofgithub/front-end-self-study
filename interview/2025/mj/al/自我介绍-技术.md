面试官您好！我叫陈源鑫，今年 25 岁，老家是广东汕头的。然后我是 23 届毕业，目前正式参加工作时间（不包含实习）是差不多两年半。

然后我的工作经历大致可以分成两段：

-   一段是我在上海百度 4 个月的实习经历
-   一段是我在 CVTE 也就是现在这家公司，算是实习差不多是 3 年多时间

然后我说一下我在这些经历里面我学到什么东西

1. 在上海实习的时候我是负责一个叫百度舆情的项目，在这个项目里面我们是有用到微前端架构，所以在这段经历我也是对微前端有一定的了解（**展示一下自己的项目**），然后我自己也是维护了一个仓库写了一个微前端的 mini 版，然后这个仓库我也是有学习设计的，我把每个学习概念用一条分支梳理

2. （**打开图**）然后在我现在这家公司里开始主要维护一个叫学生综合评价的项目，内容主要是学生一类的数据收集，然后这个项目主要难点在于它有一个学生档案模块，支持老师去编排档案内容，然后这个模块的主要能力基本都是我实现的，这个模块难点在于它的双页预览模块内容的截断，还有聚焦框的切分

-   双页预览：介绍分页思想，插桩
-   聚焦框：介绍一下聚焦模式，然后表示自己使用策略模式组织，然整个计算过程清晰一点

3. 然后是我们去年刚立项的一个项目，数据服务中台，这个项目可以支持用户导入数据源，对数据进行可视化处理，然后结合低代码技术去搭建数据看板，然后我这边主要负责了两块内容：数据处理和数据看板

-   (**打开图**)数据处理涉及到流程图的编辑，这个我是参考了现在 Dify 的流程图编辑实现，底层用的 reactFlow

    -   这个模块主要难点在于需要处理的状态比较多，这里我是抽了两个 Store 去管理，可以看两个模型，一个是任务模型（管理任务状态），一个是节点模型(管理画布节点)

-   （**打开图**）数据看板，这个看板是基于我们团队自研的低代码引擎去实现的，可以支持自由编排，还有数据源的切换，基础的属性配置、风格的切换
    -   然后关于低代码部分我也写了两篇文章（展示）

4. （**打开图**）最后是我在简历提到的这个 AI agent ，这个主要满足学校定制一些特定场景的智能体，底层我们是用 dify 平台去搭建工作流或者是 chat，我们不直接用 dify 的 ui，只用它的编排能力，ui 渲染是自己实现的方便自己去做些定制。
    - 这个模块主要难点在于怎么把流式文本内容渲染成对应的组件，这边我是用了一个 ReactStreamdown 的一个库，通过匹配对应 markdown 标签的方式去匹配对应的 react 组件完成业务渲染
    - 还有一个问题是不同的智能体虽然基本的能力是一致的，但会有些许差异，比如说文件类型的限制、大小、数量不一致、所以我是抽出了一些配置，支持不同智能体的定制
    - 还有一个是拓展能力，考虑到后续可能我们不仅仅只是接 dify，可能也接其他平台，所以我的接口是做了一层适配器，可以去适配不同的大模型接口

## 自我介绍

面试官您好 23 届毕业正式参加工作的时长到差不多是 2 年半时间 实习了也有差不多一年时间

一段是在百度实习经历 在百度实习期间主要是维护一个舆情系统 然后当时项目推出新版 我开始负责这个新版的开发 然后当时我是用微前端 micro-app 把新旧版本隔离开 在新项目开始使用 vue3

在这段实习期间对这两个技术也算是有了一些了解

micro-app 我是有自己手敲了一个 mini 版本

在这家公司我的做第一个项目是设备集控 这个项目有一个比较核心的模块是设备巡视（用 webRTC）实现的 为了能维护这个项目我也熟悉了 webRTC 和 websocket 等技术(掘金文章)

然后因为后续部门调整我被分配去做一个综合评价项目 在这个项目里我有一些性能优化经验积累(可以展示) 以及技术视角更加全面（这个系统自己的文件服务我也开始去学习一些基础的后台知识）也积累了一些复杂模块的设计经验（学生档案）这个项目我现在也一直还在维护

今年年中我们开始做了以一个数据服务平台的项目，这个项目比较有趣，这是一个可以套支持可视化数据导入、数据库操作、数据指标看板编排的系统，里面涉及到了流程图编辑和低代码、AI 对话框等技术（展示）

然后我也持续在拓展自己的能力边界，会去做一些我没有涉猎的领域进行调研学习，然后争取机会不断实践提升自己（展示自己后台开发的内容、学习的内容）

## 简历问题

### microApp 原理

-   渲染原理

1. **元素注册**：通过 Web Components 监听 `<micro-app>` 元素插入
2. **HTML 解析**：获取并解析子应用 HTML，替换标签避免冲突
3. **资源提取**：递归提取 CSS/JS 资源，从 DOM 中移除
4. **并行加载**：使用 `Promise.all` 并行加载所有资源
5. **同步等待**：通过计数器机制确保资源加载完成
6. **渲染执行**：使用 `DocumentFragment` 插入 DOM，执行 JS 代码

-   沙箱隔离原理： proxy 代理 window + with 语句改变子应用上下文 window 为 proxyWindow
-   通信原理：通过自定义标签属性注入
-   样式隔离原理：遍历 css rules 加定义前缀

### webRTC 通信过程

-   创建 RTCPeerConnection 对象(webrtc 核心对象)
-   RTCPeerConnection 绑定要传输的音视频数据

```javascript
const stream = navigator.getUserMedia({ audio: true, video: true }）
stream.getTracks().forEach((track) => {
    pc.addTrack(track, stream);
})
```

-   媒体协商(协商通信格式)
-   ICE(选择最好的链路传输音视频数据)
-   前端进行音视频渲染

### websocket 原理

-   握手：通过 http 握手后获取 socket 连接
-   通信：自定义通信协议后续通过 socket 连接进行通信
-   心跳机制：定时发送消息保持心跳

## JD 内容

### react 运行流程原理

我们就入口流程开始说起：

-   我们创建一个 React 应用 ReactDom.render(<App />, document.getElementById('root'))
-   v18 是 const root = ReactDom.createRoot(document.getElementById('root')) root.render();
-   这里的 App 本质返回的是虚拟 dom，jsx 会经过 babel/preset-react 编译成 function app () { ... retrun React.createElement()}
-   在 render 方法里会基于虚拟 dom 执行构建 reconcileRootFiber 树（根协调阶段，可中段），第一次渲染会直接 commit 挂载到页面上，副作用是 placement(挂载)
-   后续更新状态如果是通过 setState 会做局部协调 recocilieChildFiber，比对新旧 fiber 树标识出变更的副作用如 update、delete
-   比对结束后会 commit 副作用，其实就是遍历 fiber 把副作用转换为真实的 dom 操作

### 具备跨端开发经验，熟悉 React Natvie/Taro 或鸿蒙适配经验

我没有跨端经验，不过我以前大学的时候倒是写过 uniapp 还有自己也学了一点 eletron 的基本知识

#### RN(仅支持原生渲染): js 映射到原生

-   RN 启动先跑原生代码，初始化 JS 引擎和桥接层，再加载执行 JS 逻辑；
-   JS 层只负责生成界面描述（Shadow Tree），通过桥接层传给原生端；
-   原生端负责布局计算和最终的界面渲染，交互事件则反向通过桥接层传给 JS 处理。
-   整个渲染流程和 react web 渲染的流程差不多，就虚拟 dom 的类型不一样，只是 commit 阶段会通过 js bridge 桥接（c++）调用原生 api dom 操作变成了通知原生层更新原生组件

#### Taro2(支持多端): js 映射到不同平台代码

#### Taro3(支持多端): 模拟 dom、bom 环境运行时适配到不同平台

-   抽象统一接口、不同平台有不同适配器
-   运行时根据平台选择不同的适配器适配

uniApp(支持多端): js 映射到不同平台代码
Electron(桌面应用): Chromium(浏览器内核渲染 ui 和运行 js) + Node.js，主进程和渲染进程通过 IPC 通信

### 熟悉 AI 辅助开发工具（如 copilot/cusor/cladu code）的 使用与集成，具备 Prompt 优化或 MCP 相关开发经验者

#### prompt 优化

现在这里大家总结出的优化技巧有很多，STAR 法则，最重要的还是你要有规范描述清楚自己的需求就可以了，然后根据实际情况跑下来的情况去增加一些限制规则或者是一些样本不断去完善

像我自己的话经常会用 cusor 的 plan 模式让它不断对我的提示词进行优化，我自己 review 一遍如果描述清楚，边界梳理清楚就可以了

#### mcp 开发流程

## 反问

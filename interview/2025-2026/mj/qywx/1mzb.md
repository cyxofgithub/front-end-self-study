# 岗位要求
## 在线文档


### OT/CRDT算法原理与区别
 [参考](../../../../场景解决方案/多人编辑/编辑器多人协作冲突原理.md)

#### OT算法原理

同步的核心基于中心服务位置的操作转换，需要中心服务器统一处理转换操作

#### CRDT算法原理

- 操作都有唯一id和value、left、right，并按照全序规则排序(冲突时)
- 只要理解了排序就理解了同步原理

**同步的时机：基于版本向量**

例如版本向量 `{A: 5, B: 3}` 表示：当前副本已处理 A 的 1..5 和 B 的 1..3。若收到对端摘要 `{A: 4, B: 5}`，就知道：

-   我缺 B 的 4..5，需要拉取并应用；
-   对端缺 A 的 5，需要回传。



### 编辑器框架：proseMirror 

- [常见问题](../../../../编辑器/proseMirror常见问题.md)

### wasm 的优化

- Pulldown-cmark WASM Markdown 解析
- Yjs WASM 绑定（协同场景） [优化示例](../../../../编辑器/在线文档协同/code)
- Shiki WASM 语法高亮

#### wasm 快的原因

- 避开 GC 停顿：Wasm 线性内存由开发者手动管理，没有 JS 那种 “自动暂停执行回收内存” 的 GC 操作，执行过程连续无中断；
- 二进制格式 + 静态类型：大幅降低解析 / 编译成本，避免运行时类型开销；
- 接近机器码的指令集：执行效率对标 C/C++，远超动态类型的 JS（计算密集型场景）。

### canvas 优化

- 在线文档：多用户光标 / 选区 通过 Canvas 层渲染 [优化示例](../../../../编辑器/在线文档协同/code)
- 大表格混合渲染（Canvas + DOM）
  -  用 Canvas 绘制表格内容（文本、边框、背景）。
  -  仅将「当前输入焦点的单元格」用 DOM 覆盖（用于接收输入、显示光标）。

#### canvas 快的原因

- 局部重绘（最核心的优化）
  - 不要每次都 clearRect(0, 0, width, height) 擦除整个画布，只擦除「需要更新的区域」：
    - 比如光标移动：只擦除旧光标位置的小矩形（比如 20x20 像素），再在新位置绘制；
    - 比如表格滚动：只擦除滚动时 “新增的边缘区域”，复用中间已绘制的内容；
    - 效果：重绘面积从 “整个画布” 降到 “局部小区域”，消耗降低 90% 以上

-  离屏 Canvas（OffscreenCanvas）

把 “静态内容”（比如表格的表头、背景）先画在一个离屏 Canvas 上，需要时直接把离屏 Canvas 的像素 “复制” 到主 Canvas：
  - 比如大表格：表头、固定列先画在离屏 Canvas，滚动时直接 drawImage(offscreenCanvas, x, y) 复制，无需重复绘制静态内容；
  - 效果：减少重复绘制的计算量，消耗再降 50% 以上。

- 利用 requestAnimationFrame 同步帧率

**通俗比喻**

- DOM 重排：像 “重新装修整栋楼”—— 你只是想移动一个房间的家具，却要重新计算整栋楼的承重、布局，耗时极长；
- Canvas 重绘：像 “在墙上重新画一幅画的局部”—— 你只需要擦除旧的一笔，画上新的一笔，耗时极短，哪怕画的是复杂的图案

## openClaw

[openClaw相关](../../../../interview/AI/openClaw相关.md)

## Webpack/Vite 构建原理

### webpack

[详细看](../../../webpack/webpack打包原理.md)

我们从入口讲起

-   在配置 webpack 的时候我们配置 entry、output、plugin、module/rules
-   执行 webpack 后会先根据配置找到入口文件读取文件内容 利用 babel 转换成 ast 识别依赖
-   如果依赖是 module rules 的文件会根据对应的 loader 转换然后继续重复这个读取 -> ast -> 识别依赖这个过程构建出依赖图
-   依赖图构建完成后会根据依赖关系输出 chunk （中间涉及到模块转换，比如 es import 语句转换成 webpack 可识别的模块语句**webpack_require**）
-   n 个入口就有 n 个 chunk 如果有配置 split chunk 会有更多



### vite

[详细参考](../../../vite/README.md)

思路：

- 从根html触发，读取主入口文件，用 esbuild 打成少量 ESM（如一个 deps.js），放到 node_modules/.vite/。
- 当浏览器请求 url 时拦截，读取文件 → esbuild.transform(启动时未预构建部分) → 将 import 'lodash-es' 改写为 import '/node_modules/.vite/deps.js?t=...'，返回 JS。




## Monorepo

[详细参考](../../../../interview/工程化/Monorepo/Monorepo高频面试题.md)

## 微前端

[微前端、模块联邦、微前端](../../../../interview/微前端/微前端、模块联邦、微前端.md)

## egg/next

### egg

[详细参考](../../../node/egg-next/egg高频梳理.md)

### next

[详细参考](../../../node/egg-next/next高频梳理.md)

## React/Vue3、Typescript

### TypeScript

[详细参考](../../../typescript/高频梳理.md)

### React 

[详细参考](../../../../React/mini-react/README.md)

### vue（最后）


# 个人经历复习

## 自我介绍

面试官您好，我叫陈源鑫，是 23 届毕业生，目前有两年多工作的经验

整个工作经历大致可以分成两段：

-   一段是在百度四个月的实习经历
-   一段就是现在这家公司 CVTE

在百度实习的四个月里，我负责的是百度舆情系统，然后这个项目主要是通过抓取舆情数据为企业提供舆情监控、分析的能力。技术栈主体以 Vue 为主

在 CVTE 我主要负责的是魔方平台及其下面相关的教学、管理应用，这个项目主要是提供一个基座能力用于管理学校应用，解决学校应用分散、应用间数据不流通、组织不一致等问题。项目技术栈主要以 React 为主，BFF 层用的 egg

这就是我的大致经历，您看有什么想问我的

## 为什么想要换工作/离职

-   主要是我个人对自己成长的需求
    -   所以我这次找工作目的也比较明确，我想加入更有知名度的互联网公司，因为我觉得学习的环境很重要，因为在这里我能有更多的机会接触到更多优秀的同事、更前沿的技术。
    -   还有就是想做点不一样的事情，跟前面的面试官也聊到了这个岗位也有关于在线文档、webAssembly相关的内容，这些都是我过去接触得比较少的，挑战一些更有难度的事情也能帮助我更快的成长

## 在过去遇到的最大的技术挑战

近期就是我在做这个数据服务平台数据处理模块的时候，它本质其实就一个 sql 的可视化操作页面

难点主要有几个方面：

-   一个是业务的理解成本，我需要去梳理清除这些节点背后映射的是那些 sql 语句，因为只有梳理清楚这些东西你才能知道要怎么去设计每个节点的数据结构既要方便去映射到后台的 sql 语句又要方便我这边对节点位置、连线等基础状态的获取；其实这些 sql 节点都可以抽象成一个流程节点，首先要去确定好节点模型基础的属性，比如节点的位置、前后节点 id 等等，然后后台 sql 这些比较业务的描述我是统一放在一个属性里，然后后台去定义清楚，然后再一起对齐一下看看有没有问题需不需要去做调整

-   一个是技术实现的难度，首先它是可视化的流程图，它的交互相对比较复杂需要做拖拽、连线、选中等操作，那我就需要提前去调研怎么去实现它，当时我是去找一些相同场景的应用，比如像 dify、coze 了解清除他们是用什么样的技术去实现这个功能的，然后尝试去做一个 demo，看看能否满足产品需求（像 dify 用的就是 react flow，这个库其实也是现在 react 生态里实现流程图比较主流的一个库，最主要的是它能满足我们的需求，比如节点样式的自定义，然后再性能方面也支持节点的懒加载、虚拟滚动）。

-   一个是它整个模块状态也比较多，处理对流程节点的管理还需要管理任务状态、任务的运行设置、编辑的历史记录等等。当时我是划分了两个数据模型，一个是任务的模型，一个是节点模型，任务模型去处理这些全局操作，节点模型就是针对流程图的管理

-   还有就是联调成本，不同的 sql 节点拼装后数据是否正确、起码我自侧的时候要覆盖每个节点的场景去做自侧，但是这样就很麻烦，我当时是用 ai 帮我写一个节点对象转 sql 的脚本，然后我直接拿 sql 查后台的表再去做核对

-   一个是时间也比较急，我们在赶一个展会演示，mvp 思维，写好 todo

## 低代码相关

### 低代码引擎为什么要自己设计

-   阿里的 lowcode-engine 的定位是页面级的编排，所以框架会比较重，比如里面包含了对交互事件的处理、数据源的处理，定位的用户偏向有技术背景的人员，而且它对编辑器的自定义也比较有限；百度的 amis 定位更倾向于 json 配置代码，所以对编辑器的支持也比较少；而我们的场景更多是提供一些现成的组件给用户做简易的编排和属性的修改，更轻量级
-   性能问题：编辑端和渲染端耦合，编辑端需要拖拽、配置面板、选中的能力，渲染端其实只需要渲染能力
-   维护问题：这些低代码框架其实挺重的，如果真出了问题能不能及时修复，如果要自己去啃源码成本也很高

### 低代码渲染性能问题怎么优化

其实思路跟我们平时优化页面也差不多：

-   组件层面的优化：

    -   尽可能减少重排重绘
    -   减少节点数量、用缓存、防抖、批量更新限制不必要的重渲染

-   引擎层面的优化：
    -   目前我们还没有做比较系统性的性能，但是我们在设计的时候也会尽可能去避免掉一些性能问题，比如组件加载策略，我们是支持全量加载/按需加载，渲染端和编辑端的拆分，拓展层插件的按需引用。
    -   虽然我们做了一部分但是我为什么说没有系统性的优化，因为其实低代码渲染的主要性能问题在于渲染过程中 shema 的动态解析这会造成一定时间的白屏，但是我也有思路，如果我们是静态页面，可以把目前的 dom 节点做个快照。我们记录这份快照，还有通过 document.styleSheets 导出完整的样式表，下次渲染的时候就可以绕开引擎直接渲染
    -   如果是动态页面，会比较麻烦但也有可行性，将组件编译为渲染函数，下次引擎直接调用渲染函数进行渲染，避免 schema 解析时间

### 负责的低代码模块核心架构是什么？

我负责了组件库、编辑器部分模块的设计，具体涉及到到内容就是属属性面板插件、属性设置器等扩展能力，说得简单点就是如何把抽象定义和相关配置转换成真实渲染的面板和对应的属性设置器，怎么去减少重复的工作和开发效率以及一些定制化的需求




## AI 相关
### 为什么要自定义SSE
[参考](../../../../interview/2025-2026/mj/al/一面.md)

拓展强、可以支持自定义消息格式、业务状态码、ack机制、重试机制等


# 面筋

## 算法与数据结构

- 数组：
  - [去重（多种方案：Set、indexOf、reduce、Map）](../../../../interview/js/数组去重.md)
  - [扁平化（flat API、reduce+递归、stack+遍历）](../../../../interview/前端常见问题/扁平化数组的几种方式.md)
  - 排序
    - [快排 (选择基准元素并重新排列数组、递归)](../../../../interview/数据结构与算法/排序算法/快速排序.js) 
    - [归并 (利用分治法将问题分解成更小的子问题，递归解决这些子问题，然后合并结果)](../../../../interview/数据结构与算法/排序算法/归并排序.js)  
  - [二分查找(有序数组中查找目标值的索引)](../../../../interview/数据结构与算法/二分查找/二分查找.js)
- 字符串：
  - [反转 (left、right指针)](../../../../interview/数据结构与算法/2025总结/code/一、数组与字符串操作/5、反转字符串.js)
  - [无重复字符最长子串 (滑动窗口 + 哈希表)](../../../../interview/数据结构与算法/2025总结/code/一、数组与字符串操作/2、无重复字符的最长子串.js)
- 链表：
  - [反转 (存后继、指前驱、双前移)](../../../../interview/数据结构与算法/2025总结/code/三、链表操作/1、反转链表（LeetCode%20206）.js)
  - [合并 (虚拟头节点、双指针)](../../../../interview/数据结构与算法/2025总结/code/三、链表操作/2、合并两个有序链表（LeetCode%2021）.js)
  - [环检测 （快慢指针或set记录）](../../../../interview/数据结构与算法/2025总结/code/三、链表操作/3、环形链表（LeetCode%20141）.js)
- 二叉树：
  - 遍历
    - [前、中 、后](../../../../interview/数据结构与算法/树/树的遍历.js)
    - [层序](../../../../interview/数据结构与算法/2025总结/code/四、树与二叉树（前端组件层级结构）/1、二叉树的层序遍历（LeetCode%20102）.js)
  - [深度(递归或迭代)](../../../../interview/数据结构与算法/2025总结/code/四、树与二叉树（前端组件层级结构）/2、二叉树的最大深度（LeetCode%20104）.js)
  - [最近公共祖先(递归)](../../../../interview/数据结构与算法/2025总结/code/四、树与二叉树（前端组件层级结构）/3、二叉树的最近公共祖先（LeetCode%20236）.js)
- 高频手写：
  - [防抖节流(闭包 + 定时器)](../../../../interview/数据结构与算法/2025总结/code/八、前端工程化相关/1、防抖与节流函数.js)
  - [Promise 系列](../../../../interview/js/手写promise.all.md)

## ai 

### lanchain
-  [对 lanchain 理解、原理](../../../../interview/AI/LangChain理解与原理.md)

LangChain 是什么：面向 LLM 的应用编排框架，把「调用 LLM、检索、记忆、工具」等能力抽象成可组合单元，用声明式链式调用（LCEL）串联。
解决什么问题：统一多模型/多工具接口，降低 RAG、Agent 的编排与流式/批处理代码量

## js、ts 基础

### js
- [this 指向](../../../../interview/js/this指向.md) new绑定、显示绑定（call/apply/bind）、隐式绑定(对象属性)、箭头函数、函数上下文（指向全局）
- [['1','2','3'].map(parseInt) 的执行结果是：[1, NaN, NaN]。](../../../../interview/js/map与parseInt.md)

### ts

- [类型体操：实现 DeepPartial、UnionToIntersection、ReturnType 等](../../../../interview/typescript/类型体操.md)
- [类型守卫、infer 关键字应用](../../../../interview/typescript/类型守卫与infer.md)

